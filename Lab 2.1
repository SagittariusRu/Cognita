# Lab 2.1

import math

x = float(input("x = "))

if x <= 0:
    g = 3 * math.sin(x) - math.cos(x) * math.cos(x)
else:
    g = 3 * math.pow(1 + x * x, 1 / 2)

if x <= 0:
    z = (1 + x) / math.pow(1 + x ** 2, 1/ 3)
elif x > -1:
    z = -x + 2 * math.pow(math.e, -2 * x)
else:
    math.pow(math.fabs(2 - x))

print("g = {0:.4f}".format(g))
print("z = {0:.4f}".format(z))
